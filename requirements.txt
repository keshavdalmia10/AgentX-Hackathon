# Core dependencies
sqlglot>=20.0.0

# SQLite - No additional dependencies (built-in Python)

# DuckDB support (optional)
duckdb>=0.9.0

# PostgreSQL support (optional)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Web interface
flask>=2.0.0
flask-cors>=4.0.0

# BigQuery support (optional, uncomment if needed)
# google-cloud-bigquery>=3.0.0
